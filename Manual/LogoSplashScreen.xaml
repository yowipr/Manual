<Window x:Class="Manual.LogoSplashScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Width="500"
        Height="300"
        RenderOptions.BitmapScalingMode="HighQuality">
    <Grid>
        <!-- Tu contenido personalizado aquí -->
        <Border CornerRadius="10">
            <Image x:Name="img" Source="Assets/manualsplash.png" Stretch="Uniform">
                <Image.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Direction="275" Color="Black" Opacity="0.56"/>
                </Image.Effect>
            </Image>
        </Border>
    </Grid>
</Window>